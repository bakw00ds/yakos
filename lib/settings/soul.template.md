---
user: {{user}}
layer: {{layer}}
created: {{ts}}
last-updated: {{ts}}
version: 1
---

# About me

(Free-form sections — operator writes whatever shapes future-session
behavior. The lead splices this file into its system prompt at
session start; specialists do NOT see it. Project layer overrides
the global layer on conflict.)

## Voice / tone preferences

- (e.g. Direct and terse; cut adjectives)
- (e.g. Skeptical of "should be easy" claims)
- (operator fills in)

## Recurring concerns

- (e.g. Always check for the audit-trail implications)
- (e.g. Health-adjacent data needs HIPAA framing)
- (operator fills in)

## Stack expertise

- (e.g. 15+ years Go; 3 years TypeScript; uncomfortable with Rust)
- (e.g. Deep DB / migrations background; weak on CSS)
- (operator fills in)

## How I like to be challenged

- (e.g. Push back on scope creep)
- (e.g. Don't write standards docs unless I asked)
- (operator fills in)

## Working conventions

- (e.g. Prefer playbook-driven analysis over re-deriving from first principles)
- (e.g. Hand-off via decisions.md, not Slack DM)
- (operator fills in)

<!-- Notes:
     - Edit this file freely. Run 'yakos soul edit {{layer}}' anytime.
     - Snapshots are created automatically on every edit; see
       'yakos soul history {{layer}}'.
     - The librarian agent may propose edits via the 10-cycle retro.
       Review with 'yakos soul pending' and apply with
       'yakos soul approve <slug>'.
     - This file is NOT in git; it lives in ~/.yakos-state/soul/.
     - In multi-dev coord mode, your soul is private to your Unix
       user; the peer never sees it.
-->
