#!/usr/bin/env bash
# team.sh — STUB (Batch 1B).
# Will: team lifecycle subcommands. v0.1 ships with `yakos team restart`
# (archives current/, creates fresh, prints relaunch instructions; does
# NOT auto-relaunch claude in v0.1).
set -eu
echo "yakos team: not yet implemented (stub for Batch 1B)" >&2
echo "           Will: 'yakos team restart <project>' — archives current/," >&2
echo "           creates fresh, prints 'cd ... && claude --add-dir ...'." >&2
echo "           v0.1 does NOT auto-relaunch claude." >&2
exit 0
